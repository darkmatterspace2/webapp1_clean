OPTION 2:
Write a concise wiki-style article about **"Jenifer Lawrence and her frequent Scenes with some image gallery"**, modeled after Wikipedia structure. IMPORTANT : put this exact json output in a code snippet format when I can see html "content_html": with hmtl tags and code

STRUCTURE REQUIREMENTS
- Start with a short lead paragraph (summary).
- Immediately include an infobox-style section at the top:
  - Use an HTML `<table>` with key facts (location, length, opened, type, designer, coordinates).
  - Place a representative image above or beside the table using `<img>` with centered alignment.
- Follow with brief sections (History, Design, Cultural Significance).

MEDIA & LINKS
- Use img html tags for images/GIFs and video controls html tags for videos.
- Use "a" html tags for links; all must open in new tabs.
- Keep media centered and responsive with inline styles.

RULES
- Use only valid HTML inside 'content_html'.
- No markdown, no explanations, no extra text.

OUTPUT
- Output ONLY valid JSON with this EXACT schema:
{
  "title": "Article Title",
  "category": "Architecture",
  "tags": "tag1, tag2, tag3",
  "content_html": "<p>HTML content here</p>"
}

IMPORTANT
- Wrap the JSON in a single code snippet.
- 'content_html' must visibly contain raw HTML.
- No extra keys or text outside the JSON.

